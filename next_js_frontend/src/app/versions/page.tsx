export default function VersionsPage() {
	return (
		<div>
			<h1>Versions Page</h1>
			<p>This is where our product versions will be displayed, we can show a list using /versions/ GET command to fetch all of the products</p>
		</div>
	);
}
